name: frontend
description: "Ruty - Alışkanlık Takip Uygulaması"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # HTTP & API
  http: ^1.2.0
  
  # Local Storage
  shared_preferences: ^2.2.0
  
  # Charts & Graphs
  fl_chart: ^0.66.0
  
  # Date & Time
  intl: ^0.19.0
  
  # UI Components
  flutter_colorpicker: ^1.0.3
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/pets/
    - assets/characters/
    - assets/icons/
